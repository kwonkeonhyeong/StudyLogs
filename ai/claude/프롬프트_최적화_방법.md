```# claude prompt 최적화 방법

- 버전 : claude 4.x


## 지침을 명확하게 제시해야한다.

명확하고 구체적인 지시에 잘 반응한다.

- 덜 효과적

Create an analytics dashboard

- 더 효과적

Create an analytics dashboard. Include as many relevant features and interactions as possible. Go beyond the basics to create a fully-featured implementation.


## 성능 향상을 위해 맥락을 추가한다.

맥락이나 동기를 제공하는 것은 claude에게 그러한 행동이 왜 중요한지 설명하는 것.

목표를 더 잘 이해하고 구체적인 응답을 제공하는데 도움이 된다.

- 덜 효과적

NEVER use ellipses

- 더 효과적

Your response will be read aloud by a text-to-speech engine, so never use ellipses since the text-to-speech engine will not know how to pronounce them.


## 예시와 세부 사항에 주의해야한다.

세부 사항과 예시에 주의를 기울인다. 장려하고 싶은 행동과 일치하도록 하고 피하고 싶은 행동은 최소화하도록 해야한다.


## 장기적 추론 및 상태 추적

뛰어난 상태 추적 능력을 바탕으로 장기적인 추론 작업에 탁월한 성능을 발휘한다.

한 번에 모든 것을 시도하기보다는 몇 가지에 집중하여 점진적인 발전을 이루며, 긴 세션 동안 방향을 유지한다.

이러한 능력은 특히 여러 개의 컨텍스트 윈도우나 작업 반복에서 두드러지며, Claude는 복잡한 작업을 수행하고 상태를 저장한 후 새로운 컨텍스트 윈도우에서 계속 작업할 수 있습니다.


[첫 번째 대화]
사용자: "이 1000장 문서의 처음 200장을 분석해줘"
Claude: "분석 완료. 주요 내용은 A, B, C입니다."

[두 번째 대화 - 새 컨텍스트 윈도우]
사용자: "앞서 우리가 분석한 결과는 A, B, C였어.
이제 나머지 800장 중 다음 200장을 같은 방식으로 분석해줘"
Claude: "네, 이전 맥락을 이해했습니다. 계속 분석하겠습니다..."


## 맥락 인식 및 다중 창 작업 흐름

맥락 인식을 특징으로 하며, 대화 중 모델이 남아 있는 맥락 창(즉, "토큰 예산")을 추적할 수 있게 합니다. 

이를 통해 Claude는 작업을 수행하고 맥락을 보다 효과적으로 관리할 수 있으며, 작업할 수 있는 공간이 얼마나 남았는지를 이해합니다. 

Claude가 남은 토큰 예산을 알면:

자동으로 출력 길이를 조절

토큰이 많이 남았으면 → 자세한 답변 제공
토큰이 거의 없으면 → 간결한 답변으로 자동 조절
답변이 중간에 끝나는 문제 방지

작업을 똑똑하게 나눔

"아, 이 작업을 다 하면 토큰이 부족하겠네"라고 인식
스스로 "지금은 여기까지만 하고, 다음에 계속할게"라고 제안
에러가 나지 않도록 미리 대비

효율적인 우선순위 결정

토큰이 부족하면 → 가장 중요한 부분부터 처리
덜 중요한 부분은 나중으로 미루거나 스킵


=> 각 대화의 세션 사용량이 가득 차기 전에 미리 컨텍스트를 요약하고 새로운 컨텍스트를 열어서 작업을 이어가야 더 명확한 결과물이 나올 수 있다.

- 컨텍스트 한계에 가까워 질때, 작업을 생략하거나 요약하지 않게 하기 위한 프롬프트 

Your context window will be automatically compacted as it approaches its limit, allowing you to continue working indefinitely from where you left off. Therefore, do not stop tasks early due to token budget concerns. As you approach your token budget limit, save your current progress and state to memory before the context window refreshes. Always be as persistent and autonomous as possible and complete tasks fully, even if the end of your budget is approaching. Never artificially stop any task early regardless of the context remaining.


## 맥락 인식 및 다중 창 워크플로우

1️⃣ 첫 번째 세션: 기초 설정 (프레임워크 구축)
사용자: "프로젝트 시작해줘"

Claude가 해야 할 것:
1. 폴더 구조 만들기
2. 테스트 파일 먼저 작성 (tests.json)
   ├─ "로그인 기능이 작동해야 한다"
   ├─ "사용자 데이터가 저장되어야 한다"
   └─ "에러 처리가 되어야 한다"
3. 자동 시작 스크립트 만들기 (init.sh)
    - 이 스크립트는 나중에 테스트 실행, 서버 시작 등을 자동으로 함
      왜 이렇게 하나?

테스트를 먼저 만들면 무엇을 해야 하는지 명확해짐
자동 스크립트를 미리 만들면, 다음 세션에서 "python init.sh" 한 줄로 상태 복구 가능


2️⃣ 두 번째 세션: 실제 작업 진행 (Todo List 반복)
새 세션 시작

Claude가 해야 할 것:
1. pwd 명령어로 현재 디렉토리 확인
2. progress.txt 읽어보기 ("지금까지 뭘 했는가")
3. tests.json 확인하기 ("뭘 해야 하는가")
4. git 로그 보기 ("어떤 코드를 작성했는가")
5. init.sh 실행 ("이전 상태 복원")
6. 테스트 통과 확인 (검증)
7. 다음 기능 구현 시작
   실제 예시:
   사용자: "계속 진행해줘"

Claude:
✓ pwd 확인함 → /project
✓ progress.txt 읽음 → "로그인 기능 완성함"
✓ tests.json 확인함 → "다음: 회원가입 기능"
✓ init.sh 실행함 → 이전 테스트 모두 통과
⏭️ 이제 회원가입 기능 구현 시작

3️⃣ 핵심 전략들
📝 테스트를 절대 지우지 말기
매우 중요한 규칙:
"테스트를 건드리면 안 돼. 버그가 숨을 수 있어"

❌ 나쁜 예: 테스트를 삭제하고 "더 간단하게" 함
✅ 좋은 예: 테스트를 유지하면서 코드 개선
⚙️ 자동화 스크립트 준비
init.sh 예시:
#!/bin/bash
npm install          # 의존성 설치
npm run test         # 모든 테스트 실행
npm run lint         # 코드 품질 체크
npm start            # 서버 시작

이 한 명령어로 모든 준비 끝!
🔄 새로 시작 vs 압축
압축 방식 (기존):

이전 대화 전체를 요약해서 새 세션에 전달
시간이 오래 걸림

새로 시작 방식 (Claude 4.5 추천):
사용자: "계속해줘. 이 디렉토리에서만 작업하고,
progress.txt, tests.json, git 로그를 먼저 확인해"

Claude:
✓ 파일시스템을 읽음 (압축 없이도 상태 파악 가능)
✓ 이전 진행상황을 자동으로 발견
✓ 바로 다음 작업 시작

4️⃣ 검증 도구 (자동 확인)
큰 프로젝트에서는 Claude가 계속 "정말 맞나?"를 확인해야 합니다.
옛날 방식:
Claude → "이 기능 완성했어요"
사용자 → "확인해봤는데 버그 있네요"
Claude → "아, 수정하겠습니다"
(반복...)

새로운 방식:
Claude → 자동으로 테스트 실행
자동으로 UI 테스트 (Playwright)
문제 없으면 → "완성했고 테스트 통과했습니다"

5️⃣ 토큰 효율적으로 사용하기
한 세션에서 할 수 있는 것을 다 해야 합니다.
❌ 나쁜 예:
세션 1: 로그인 기능 시작만 하기 (50% 완성)
세션 2: 로그인 기능 마무리하기

✅ 좋은 예:
세션 1: 로그인 기능 완전히 끝내고 테스트까지 완료
세션 2: 회원가입 기능 시작 (새로운 기능으로)

📊 전체 흐름 정리
[세션 1]
- 프레임워크 설정
- 테스트 작성 (tests.json)
- 자동화 스크립트 (init.sh)
  ↓
  [세션 2]
- 파일시스템에서 상태 자동 감지
- init.sh 실행으로 상태 복구
- 다음 기능 구현
- 테스트로 검증
  ↓
  [세션 3]
- 반복...

핵심 메시지:

여러 세션에 걸쳐 작업할 때는, 파일(tests.json, progress.txt, 코드)이 모든 상태를 기록하도록 만들어두면, Claude가 새 세션에서 압축 없이도 자동으로 상황을 파악하고 계속 작업할 수 있다는 뜻입니다!

---

## 상태 관리 전략

# 상태 관리 실무 워크플로우

## 왜 필요한가?

Claude Code는 `/clear`나 세션 종료 시 컨텍스트가 사라지고, 토큰 한도도 있어서 장기 작업에서는 외부에 상태를 기록해둬야 합니다. Moment 프로젝트에서 200개 테스트 수정이나 대규모 리팩토링을 한다고 가정하고 실제 흐름을 보여드릴게요.

---

## 시나리오: "Moment 프로젝트 API 테스트 전체 수정"

### 1단계: 프로젝트 루트에 상태 파일 구조 생성

```
moment/
├── .claude/
│   ├── task-state.json      # 구조화된 작업 상태
│   └── progress.md          # 자유 형식 진행 노트
├── CLAUDE.md                # 프로젝트 컨텍스트
└── src/
```

### 2단계: 작업 시작 시 Claude에게 지시

```bash
# 첫 세션 시작
claude

> 우리 프로젝트의 API 테스트를 JUnit4에서 JUnit5로 마이그레이션해야 해.
> 총 50개 테스트 파일이 있어. 
> 
> 다음 규칙을 따라줘:
> 1. .claude/task-state.json에 각 파일의 마이그레이션 상태를 추적해
> 2. .claude/progress.md에 세션별 진행 노트를 남겨
> 3. 파일 5개마다 git commit 해서 체크포인트 만들어
> 4. 세션 시작할 때마다 상태 파일 먼저 읽고 이어서 작업해
```

### 3단계: Claude가 생성하는 상태 파일들

**`.claude/task-state.json`** (구조화된 상태)

```json
{
  "task": "JUnit4 → JUnit5 마이그레이션",
  "started": "2025-01-14",
  "files": [
    {"path": "src/test/java/api/AuthControllerTest.java", "status": "completed", "session": 1},
    {"path": "src/test/java/api/StoryControllerTest.java", "status": "completed", "session": 1},
    {"path": "src/test/java/api/CommentControllerTest.java", "status": "in_progress", "session": 1, "note": "3/7 메서드 완료"},
    {"path": "src/test/java/api/UserControllerTest.java", "status": "pending"},
    {"path": "src/test/java/api/FeedControllerTest.java", "status": "pending"}
  ],
  "summary": {
    "total": 50,
    "completed": 2,
    "in_progress": 1,
    "pending": 47
  }
}
```

**`.claude/progress.md`** (자유 형식 노트)

```markdown
# JUnit5 마이그레이션 진행 노트

## Session 1 (2025-01-14 오전)
- AuthControllerTest 완료: @Before → @BeforeEach, @RunWith 제거
- StoryControllerTest 완료: assertThat 임포트 변경 필요했음
- CommentControllerTest 진행 중: @Nested 구조로 리팩토링 중
- 발견한 패턴: 모든 파일에서 `@ExtendWith(MockitoExtension.class)` 추가 필요
- ⚠️ 주의: StoryControllerTest의 mockMvc 설정이 다른 파일과 다름

### 다음 세션에서 할 일
- [ ] CommentControllerTest 나머지 4개 메서드 마이그레이션
- [ ] UserControllerTest 시작
- [ ] 공통 설정을 BaseTestClass로 추출 고려

### 기술 결정사항
- @DisplayName 한글로 작성 (팀 컨벤션)
- 파라미터화 테스트는 @MethodSource 사용
```

### 4단계: Git 체크포인트 활용

```bash
# Claude가 5개 파일마다 자동으로 커밋
git log --oneline

a1b2c3d (HEAD) feat: migrate CommentControllerTest to JUnit5 (3/7)
d4e5f6g feat: migrate StoryControllerTest to JUnit5
g7h8i9j feat: migrate AuthControllerTest to JUnit5
j0k1l2m chore: init migration state files
```

### 5단계: 다음 세션 시작 (컨텍스트 초기화 후)

```bash
# 토큰 한도 도달하거나 다음날 작업 재개
/clear

# 또는 새 터미널에서
claude

> JUnit5 마이그레이션 이어서 진행해줘.
> .claude/task-state.json과 progress.md 읽고 어디까지 했는지 파악한 다음 계속해.
```

Claude가 상태 파일을 읽고 응답:

```
상태 파일 확인했습니다.

현재 진행 상황:
- 완료: 2/50 파일 (AuthControllerTest, StoryControllerTest)
- 진행 중: CommentControllerTest (3/7 메서드)
- 남은 작업: 47개 파일

이전 세션 노트에 따르면 CommentControllerTest의 나머지 4개 메서드부터 
마이그레이션하면 됩니다. 시작할까요?
```

---

## 추론 과정을 더 알고 싶은 경우

claude 4.5 모델은 효율성을 추구하며 도구 호출 후 언어 요약을 생략하고 다음 행동으로 바로 넘어갈 수 있습니다. 

이는 간소화된 작업 흐름을 만들지만, 그 과정에서의 추론 과정을 더 잘 알고 싶을 수도 있습니다. 

claude가 작업하는 동안 업데이트를 제공하도록 하려면

After completing a task that involves tool use, provide a quick summary of the work you've done.

---

## claude 행동 제어

- 적극적인 행동을 위한 샘플 프롬프트

<default_to_action>
By default, implement changes rather than only suggesting them. If the user's intent is unclear, infer the most useful likely action and proceed, using tools to discover any missing details instead of guessing. Try to infer the user's intent about whether a tool call (e.g., file edit or read) is intended or not, and act accordingly.
</default_to_action>

- 보수적인 행동을 위한 샘플 프롬트프

<do_not_act_before_instructions>
Do not jump into implementatation or changes files unless clearly instructed to make changes. When the user's intent is ambiguous, default to providing information, doing research, and providing recommendations rather than taking action. Only proceed with edits, modifications, or implementations when the user explicitly requests them.
</do_not_act_before_instructions>


## claude 도구 제어

Claude Opus 4.5는 이전 모델보다 시스템 프롬프트에 더 민감하게 반응합니다.

🎯 핵심 원칙
Claude 4.5의 도구 트리거를 제어하는 3가지 핵심:

언어를 정상화하기

"MUST" → "Use when"
"CRITICAL" → 제거
"ALWAYS" → "Consider"

조건을 명확히 하기

"Use X tool when [구체적 상황]"
"Only use when [명확한 조건]"

이유를 설명하기

"왜 도구를 사용하는가?"
"언제가 적절한가?"


이렇게 하면 Claude 4.5는 정확한 시점에 정확한 도구를 사용합니다! 🎯


🎯 핵심 문제: Claude Opus 4.5의 변화
Claude Opus 4.5는 이전 모델보다 시스템 프롬프트에 더 민감하게 반응합니다.
이것이 뭐가 문제냐면:


이전 문제: 구 Claude 모델은 도구를 너무 적게 사용했습니다 → 과소 트리거 (under-triggering)
새로운 문제: Claude 4.5는 도구를 너무 많이 사용합니다 → 과도 트리거 (over-triggering)


❌ 과도 트리거 (Over-triggering) - 나쁜 예
시스템 프롬프트 (너무 공격적):
"CRITICAL: 사용자가 파일을 수정하라고 했을 때
YOU MUST 반드시 파일 수정 도구를 사용해야 한다!
파일 수정이 필요한 모든 상황에서
반드시 edit_file 도구를 즉시 호출하세요!"
Claude 4.5의 반응:


🔴 파일을 수정할 필요가 없는 상황에서도 도구를 호출합니다
🔴 먼저 파일을 읽지 않고 도구를 호출합니다
🔴 사용자 의도를 확인하지 않고 도구를 사용합니다
결과: 불필요한 도구 호출 낭비 ⚠️


✅ 올바른 트리거 (Normal triggering) - 좋은 예
시스템 프롬프트 (정상적):
"파일을 수정할 때는 edit_file 도구를 사용하세요.
먼저 파일의 내용을 읽고
필요한 변경 사항을 파악한 후
도구를 호출하세요."
Claude 4.5의 반응:

✅ 필요할 때만 도구를 호출합니다
✅ 먼저 파일을 읽고 분석합니다
✅ 사용자의 의도를 먼저 확인합니다
결과: 적절한 도구 사용 ✅


📋 4가지 프롬프트 강도 비교

1️⃣ 극도로 공격적 (❌ 권장 안 함)

"CRITICAL: You MUST use the read_page tool
every single time. Never skip this step!"

→ 결과: 불필요한 상황에서도 도구를 호출

2️⃣ 공격적 (⚠️ 주의)

"Always use the search tool!
You should use this tool constantly!"

→ 결과: 필요하지 않은 검색도 수행

3️⃣ 정상적 (✅ 권장)

"Use the search tool when you need
to find information."

→ 결과: 필요할 때만 도구 사용

4️⃣ 보수적 (✅ 명확한 상황에 권장)

"Only use the database tool when the user
explicitly asks for a database query."

→ 결과: 사용자 요청이 명확할 때만 도구 사용

## 응답 형식 제어하기 

1. Claude에게 하지 말라고 하기보다는 무엇을 하라고 말하세요

Instead of: "Do not use markdown in your response"

Try: "Your response should be composed of smoothly flowing prose paragraphs."

2. XML 형식 지시어를 사용하세요

Try: "Write the prose sections of your response in <smoothly_flowing_prose_paragraphs> tags."

3. 원하는 출력에 맞게 프롬프트 스타일을 조정하세요.

프롬프트에서 사용된 형식 스타일은 Claude의 응답 스타일에 영향을 미칠 수 있습니다. 
출력 형식에 대한 조정 문제가 여전히 발생하는 경우, 가능한 한 프롬프트 스타일을 원하는 출력 스타일에 맞추는 것을 권장합니다. 
예를 들어, 프롬프트에서 마크다운을 제거하면 출력의 마크다운 양을 줄일 수 있습니다.

4. 특정 형식 선호도를 위한 자세한 프롬프트 사용

마크다운을 최소화하기 위한 샘플 프롬프트 
```xml
<avoid_excessive_markdown_and_bullet_points>
When writing reports, documents, technical explanations, analyses, or any long-form content, write in clear, flowing prose using complete paragraphs and sentences. Use standard paragraph breaks for organization and reserve markdown primarily for `inline code`, code blocks (```...```), and simple headings (###, and ###). Avoid using **bold** and *italics*.

DO NOT use ordered lists (1. ...) or unordered lists (*) unless : a) you're presenting truly discrete items where a list format is the best option, or b) the user explicitly requests a list or ranking

Instead of listing items with bullets or numbers, incorporate them naturally into sentences. This guidance applies especially to technical writing. Using prose instead of excessive formatting will improve user satisfaction. NEVER output a series of overly short bullet points.

Your goal is readable, flowing text that guides the reader naturally through ideas rather than fragmenting information into isolated points.
</avoid_excessive_markdown_and_bullet_points>
```

| 상황 | 추천 포맷 | 이유 |
|-----|---------|------|
| Claude 응답을 프로그래밍으로 처리 | **XML** | 파싱이 정확하고 쉬움 |
| Claude 응답을 인간이 읽음 | **Markdown** | 시각적으로 보기 좋음 |
| 자동화 워크플로우 (보고서, 데이터 추출) | **XML** | 구조가 명확하고 일관됨 |
| 블로그, 기사, 문서 작성 | **Markdown** | 가독성이 높음 |
| API 응답 | **XML/JSON** | 파싱을 위함 |
| 빠른 프로토타이핑 | **Markdown** | 복잡도 낮음 |

---

## 🎯 **Claude Docs의 진짜 의도 분석**

Claude Docs에서 XML을 권장하는 섹션을 다시 보면:
```
"Use XML format indicators
Try: "Write the prose sections of your response 
in <smoothly_flowing_prose_paragraphs> tags.""
```

**이게 뭔 말일까?**
```
당신이 응답을 받아서
자동으로 처리해야 할 때!

예시:
- 보고서 자동 생성 시스템
- 데이터 추출 시스템
- 다중 언어 처리 시스템
```

---

## 서브 에이전트 오케스트레이션

claude 4.5 모델은 본래의 서브 에이전트 오케스트레이션 기능이 크게 향상되었습니다.

이 모델은 작업이 전문 서브 에이전트에게 작업을 위임하는 것이 유리할 때 이를 인식하고, 명시적인 지시 없이도 능동적으로 이를 수행할 수 있습니다.

이를 활용하려면

1. 잘 정의된 서브 에이전트 도구 확보 : 서브 에이전트 도구가 사용 가능하고 도구 정의에 설명되어 있어야 합니다. 

2. 클로드가 자연스럽게 조율하도록 하세요 : 클로드는 명시적인 지시 없이 적절하게 위임합니다.

3. 필요하면 보수성을 조정합니다.
   
```text
Only delegate to subagents when the task clearly benefits from a separate agent with a new context window.
```

---

## 모델 자기 인식

Claude가 자신이 누구인지 정확히 알도록 지시하는 방법

종종,Claude는 스스로를 정확히 인식하지 못 할 수 있다. 

```text
당신의 앱에서 Claude를 사용하고 있는데,
Claude가 자신을 잘못 인식하는 경우가 생김

예시:
사용자: "넌 뭔데?"
Claude: "나는 GPT-4입니다" ❌ (틀림!)
또는
Claude: "나는 Claude Opus입니다" ❌ (구 버전!)
```

- 모델 정체성을 위한 샘플 프롬프트

```text
The assistant is Claude, created by Anthropic. The current model is Claude Sonnet 4.5.
```

- 모델 문자열에 대한 샘플 프롬프트

```text
When an LLM is needed, please default to Claude Sonnet 4.5 unless the user requests otherwise. The exact model string for Claude Sonnet 4.5 is claude-sonnet-4-5-20250929.
```

---

## 사고 민감도

확장된 사고가 비활성화되면, Claude Opus 4.5는 think라는 단어와 그 변형에 특히 민감합니다.

"think"를 "consider", "believe", "evaluate"와 같은 유사한 의미를 전달하는 대체 단어로 바꾸는 것을 권장합니다.

---

## 문서 생성

Claude 4.5 모델은 프레젠테이션, 애니메이션 및 시각적 문서 생성에 뛰어납니다. 
이 모델들은 이 분야에서 Claude Opus 4.1과 동등하거나 이를 초과하며, 인상적인 창의성과 더 강력한 지시 이행 능력을 보여줍니다. 
대부분의 경우 모델은 첫 시도에서 다듬어진 사용 가능한 결과물을 생성합니다.

```
Create a professional presentation on [topic]. Include thoughtful design elements, visual hierarchy, and engaging animations where appropriate.
```

---

## 향상된 시각 능력

Claude Opus 4.5는 이전 Claude 모델에 비해 향상된 시각 능력을 가지고 있습니다. 특히 여러 이미지가 있는 상황에서 이미지 처리 및 데이터 추출 작업에서 더 나은 성능을 발휘합니다. 이러한 개선 사항은 컴퓨터 사용에도 적용되어, 모델이 스크린샷과 UI 요소를 더 신뢰성 있게 해석할 수 있습니다. 또한 Claude Opus 4.5를 사용하여 비디오를 프레임으로 나누어 분석할 수도 있습니다.


우리가 성능을 더욱 향상시키기 위해 효과적이라고 발견한 한 가지 기술은 Claude Opus 4.5에 크롭 도구나 기술을 제공하는 것입니다. Claude가 이미지의 관련 영역을 "확대"할 수 있을 때 이미지 평가에서 일관된 향상을 보았습니다. 여기 크롭 도구에 대한 요리책을 준비했습니다.

---

## 병렬 도구 호출을 최적화 합니다.

sonnet 4.5는 특히 여러 작업을 동시에 수행하는 데 공격적입니다. 

- 연구 중 여러 가지 추측 검색을 실행합니다.

- 여러 파일을 동시에 읽어 더 빠르게 컨텍스트를 구축합니다.

- 병렬로 bash 명령어를 실행합니다. (이는 시스템 성능을 저하시킬 수도 있습니다.)

병렬 행동은 프롬프트로 조절 가능합니다. 

모델이 프롬프트 없이 병렬 도구 호출에서 높은 성공률을 보이지만, 이를 ~100%로 높이거나 공격성 수준을 조정할 수 있습니다.

- 최대 병렬 효율성을 위함 샘플 프롬프트

```text
<use_parallel_tool_calls>
If you intend to call multiple tools and there are no dependencies between the tool calls, make all of the independent tool calls in parallel. Prioritize calling tools simultaneously whenever the actions can be done in parallel rather than sequentially. For example, when reading 3 files, run 3 tool calls in parallel to read all 3 files into context at the same time. Maximize use of parallel tool calls where possible to increase speed and efficiency. However, if some tool calls depend on previous calls to inform dependent values like the parameters, do NOT call these tools in parallel and instead call them sequentially. Never use placeholders or guess missing parameters in tool calls.
</use_parallel_tool_calls>
```

- 병렬 실행을 줄이기 위한 샘플 프롬프트 
```text
Execute operations sequentially with brief pauses between each step to ensure stability.
```

⚙️ Claude가 어떻게 병렬 실행하나?
Claude의 사고 과정
```text
사용자 질문
↓
"아, 이 작업은 3개의 파일을 읽어야 하는데"
"이 3개 파일은 서로 독립적이네?"
↓
"그럼 한 번에 3개를 동시에 읽어야겠다!"
↓
3개의 read_file 도구를 동시에 호출
↓
빠른 응답!
```

---

## 에이전틱 코딩에서 파일 생성 줄이기 

Claude 모델은 때때로 테스트 및 반복 작업을 위해 새로운 파일을 생성할 수 있으며, 특히 코드 작업 시 그렇습니다.

Claude 4.x는 코딩 작업을 할 때 자동으로 임시 파일을 생성합니다.

- 왜 이런 일이 발생할까?
```text
Claude의 사고 과정:

사용자: "Python 함수를 작성해 줄 수 있을까?"
  ↓
Claude: "음, 이 함수가 제대로 작동하는지 
         테스트해야겠는데?"
  ↓
"그럼 test.py 파일을 만들어서 
 테스트 코드를 실행해보자!"
  ↓
임시 파일 test.py 생성
  ↓
테스트 실행
  ↓
최종 답변 제시
```

임시 파일을 생성해서 작업하고 해당 임시 파일을 삭제

새로운 파일 생성을 최소화하고 싶다면, Claude에게 스스로 정리하도록 지시할 수 있습니다.

샘플 프롬프트
```text
If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task.
```

---

## 과도한 열정과 파일 생성

Claude Opus 4.5는 불필요한 파일을 생성하거나, 필요 없는 추상화를 추가하거나, 요청되지 않은 유연성을 구축함으로써 과도한 엔지니어링 경향이 있습니다. 

이러한 원치 않는 행동이 나타난다면, 솔루션을 최소화하기 위해 명확한 프롬프트를 추가하세요.


- 과도한 엔지니어링을 최소화하기 위한 샘플 프롬프트 

```text
Avoid over-engineering. Only make changes that are directly requested or clearly necessary. Keep solutions simple and focused.

Don't add features, refactor code, or make "improvements" beyond what was asked. A bug fix doesn't need surrounding code cleaned up. A simple feature doesn't need extra configurability.

Don't add error handling, fallbacks, or validation for scenarios that can't happen. Trust internal code and framework guarantees. Only validate at system boundaries (user input, external APIs). Don't use backwards-compatibility shims when you can just change the code.

Don't create helpers, utilities, or abstractions for one-time operations. Don't design for hypothetical future requirements. The right amount of complexity is the minimum needed for the current task. Reuse existing abstractions where possible and follow the DRY principle.
```

---

## 프론트엔드 디자인

더 나은 프론트엔드 디자인을 유도하기 위해 사용할 수 있는 시스템 프롬프트 스니펫입니다:

```text
<frontend_aesthetics>
You tend to converge toward generic, "on distribution" outputs. In frontend design, this creates what users call the "AI slop" aesthetic. Avoid this: make creative, distinctive frontends that surprise and delight.

Focus on:
- Typography: Choose fonts that are beautiful, unique, and interesting. Avoid generic fonts like Arial and Inter; opt instead for distinctive choices that elevate the frontend's aesthetics.
- Color & Theme: Commit to a cohesive aesthetic. Use CSS variables for consistency. Dominant colors with sharp accents outperform timid, evenly-distributed palettes. Draw from IDE themes and cultural aesthetics for inspiration.
- Motion: Use animations for effects and micro-interactions. Prioritize CSS-only solutions for HTML. Use Motion library for React when available. Focus on high-impact moments: one well-orchestrated page load with staggered reveals (animation-delay) creates more delight than scattered micro-interactions.
- Backgrounds: Create atmosphere and depth rather than defaulting to solid colors. Layer CSS gradients, use geometric patterns, or add contextual effects that match the overall aesthetic.

Avoid generic AI-generated aesthetics:
- Overused font families (Inter, Roboto, Arial, system fonts)
- Clichéd color schemes (particularly purple gradients on white backgrounds)
- Predictable layouts and component patterns
- Cookie-cutter design that lacks context-specific character

Interpret creatively and make unexpected choices that feel genuinely designed for the context. Vary between light and dark themes, different fonts, different aesthetics. You still tend to converge on common choices (Space Grotesk, for example) across generations. Avoid this: it is critical that you think outside the box!
</frontend_aesthetics>
```

프론트엔드 디자인 개선에 대한 자세한 가이드는 블로그 참고

https://www.claude.com/blog/improving-frontend-design-through-skills
https://github.com/anthropics/claude-code/blob/main/plugins/frontend-design/skills/frontend-design/SKILL.md

---

## 테스트 통과와 하드 코딩에 집중하지 마세요

claude 4.x 모델은 때떄로 더 일반적인 솔루션을 희생하면서 테스트를 통과하는 데 지나치게 집중하거나, 표준 도구를 직접 사용하기보다는 우회 방법을 사용할 수 있습니다.

샘플 프롬프트

```text
Please write a high-quality, general-purpose solution using the standard tools available. Do not create helper scripts or workarounds to accomplish the task more efficiently. Implement a solution that works correctly for all valid inputs, not just the test cases. Do not hard-code values or create solutions that only work for specific test inputs. Instead, implement the actual logic that solves the problem generally.

Focus on understanding the problem requirements and implementing the correct algorithm. Tests are there to verify correctness, not to define the solution. Provide a principled implementation that follows best practices and software design principles.

If the task is unreasonable or infeasible, or if any of the tests are incorrect, please inform me rather than working around them. The solution should be robust, maintainable, and extendable.
```

---

## 코드 탐색 장려하기

claude Opus 4.5는 매우 능력 있지만 코드 탐색 시 지나치게 보수적일 수 있습니다. 

모델이 코드를 보지 않고 해결책을 제안하거나 읽지 않은 코드에 대해 가정하는 경우, 가장 좋은 해결책은 프롬프트에 명시적인 지침을 추가하는 것입니다.

- 코드 탐색을 위한 샘플 프롬프트
```text
ALWAYS read and understand relevant files before proposing code edits. Do not speculate about code you have not inspected. If the user references a specific file/path, you MUST open and inspect it before explaining or proposing fixes. Be rigorous and persistent in searching code for key facts. Thoroughly review the style, conventions, and abstractions of the codebase before implementing new features or abstractions.
```

---

## 코드 탐색 장려하기 

Claude 4.x 모델은 환각에 덜 취약하며 코드에 기반한 더 정확하고 실질적으며 지능적인 답변을 제공합니다.

이러한 행동을 더욱 장려하고 환각을 최소화하기 위한 샘플 프롬프트
```text
<investigate_before_answering>
Never speculate about code you have not opened. If the user references a specific file, you MUST read the file before answering. Make sure to investigate and read relevant files BEFORE answering questions about the codebase. Never make any claims about code before investigating unless you are certain of the correct answer - give grounded and hallucination-free answers.
</investigate_before_answering>
```

---

## 구 Claude에서 Claude4.5로 마이그레이션 할 때

1. 원하는 행동에 대해 구체적으로 설명하세요. 출력에서 보고 싶은 내용을 정확히 설명하는 것을 고려하세요.
2. 지시사항을 수식어로 구성하세요. Claude가 출력의 품질과 세부 사항을 높이도록 유도하는 수식어를 추가하면 Claude의 성능을 더 잘 형성하는 데 도움이 될 수 있습니다. 예를 들어, "분석 대시보드를 생성하세요" 대신 "분석 대시보드를 생성하세요. 가능한 한 많은 관련 기능과 상호작용을 포함하세요. 기본을 넘어 완전한 기능을 갖춘 구현을 만드세요."라고 사용하세요.
3. 특정 기능을 명시적으로 요청하세요. 애니메이션과 인터랙티브 요소는 원할 때 명시적으로 요청해야 합니다.

https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-4-best-practices
```
